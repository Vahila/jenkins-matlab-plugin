name: using mpm to install MATLAB
on: push
jobs:
  my-job:
    name: Run System tests
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Setup MATLAB
        id: setup-matlab
        uses: matlab-actions/setup-matlab@v2

      - name: Setup R2023b MATLAB
        id: setup-matlab-23b
        uses: matlab-actions/setup-matlab@v2
        with:
          release: R2023b

      - name: Running tests
        run: mvn test-compile failsafe:integration-test
        env:
          MATLAB_ROOT: ${{ steps.setup-matlab.outputs.matlabroot }}
          MATLAB_ROOT_22b: ${{ steps.setup-matlab-23b.outputs.matlabroot }}
          MLM_LICENSE_TOKEN:  ${{ secrets.MLM_LICENSE_TOKEN}}
